import React from 'react';
import PropTypes from 'prop-types';

const FirstComponent = props => {
    return (
        <div>
            This is my first example component
        </div>
    );
};

FirstComponent.propTypes = {};

export default FirstComponent;


